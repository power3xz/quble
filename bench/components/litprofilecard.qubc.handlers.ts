// PoC 확인용. const handlers = { 안에서 따옴표를 열면 fullname 후보가 떠야 한다.
const handlers = {
  // 여기서 ' 입력 -> HOVER, MainThumb.CLICK_THUMBNAIL 등 자동완성
  'SecondStat.CLICK_STAT': () => {
  }
};

export default handlers;
