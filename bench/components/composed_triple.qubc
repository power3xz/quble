use InnerPair from "./inner_pair.qubc"

component ComposedTriple {
  props { c1, c2, c3, a, b, c, d }
  template {
    div() {
      @if (c1) {
        InnerPair(c2={c2} c3={c3} a={a} b={b} c={c}) {}
      }
      @else {
        p() { {d} }
      }
    }
  }
}
