{
  "name": "quble-build",
  "private": true,
  "type": "module",
  "description": "Quble 빌드 파이프라인 - quble 컴파일 후 짝 핸들러(.qubc.handlers.ts)를 esbuild로 번들해 리소스화하고 manifest에 등록.",
  "bin": {
    "quble-build": "./build-preview.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
