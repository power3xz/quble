// litprofilecard.qubc의 이벤트 핸들러. Handlers 타입은 확장이 짝 .qubc.d.ts로 생성한다
// (파일을 열거나 .qubc를 저장하면 갱신). 키·data·params.context가 타입으로 강제된다.
import type { Handlers } from "./litprofilecard.qubc";

const handlers: Partial<Handlers> = {

};

export default handlers;
