component InnerPair {
  props { c2: bool, c3: bool, a: string, b: string, c: string }
  template {
    @if (c2) {
      @if (c3) {
        p() { {a} }
      }
      @else {
        p() { {b} }
      }
    }
    @else {
      p() { {c} }
    }
  }
}
