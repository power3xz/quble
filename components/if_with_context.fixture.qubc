component IfWithContext {
  props { cond: bool, a: string, b: string }
  contexts { Area { label: a } }
  template {
    div() {
      @if (cond) {
        @with Area {
          p() { {a} }
        }
      }
      @else {
        p() { {b} }
      }
    }
  }
}
