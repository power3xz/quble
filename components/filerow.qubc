use "./filerow.css"

component FileRow {
  props { name: string, isEntry: bool, isEditing: bool, isPreviewing: bool, hasError: bool }
  events {
    CLICK_FILE({ })
    CLICK_PREVIEW({ })
  }
  template {
    div(class="filerow" data-editing={isEditing} data-error={hasError}) {
      @if (isPreviewing) {
        span(class="filerow__live" title="미리보기 중") { "*" }
      }
      @else {
        span(class="filerow__live" /)
      }
      button(class="filerow__name" @click:CLICK_FILE) { {name} }
      @if (hasError) {
        span(class="filerow__error" title="컴파일 에러") { "!" }
      }
      @if (isEntry) {
        button(class="filerow__preview" title="preview" @click:CLICK_PREVIEW) { "run" }
      }
    }
  }
}
