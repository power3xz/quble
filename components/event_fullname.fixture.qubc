component Card {
  template {
    div() {
      Toggle(label="A" on="off" /)
    }
  }
}

component Toggle {
  props { label: string, on: string }
  events { TOGGLE({ label, on }) }
  template {
    button(@click:TOGGLE) { {label} }
  }
}
