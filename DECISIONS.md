# Decisions

설계 결정의 기록. **무엇을 정했나**가 아니라 **왜 그렇게 정했나**와 **무엇을 버렸나**를 남긴다.
지금 상태(무엇이 어떻게 동작하나)는 DESIGN.md / SYNTAX.md / BYTECODE.md가 갖는다.

기록이 필요한 이유는 하나다 - 같은 논의를 다시 하지 않기 위해서다. 버린 안을 왜 버렸는지가
남아 있지 않으면, 그 안은 좋아 보이는 채로 다시 올라온다.

절 번호를 쓰지 않는다. 항목이 늘고 줄 때 번호가 밀리면 다른 문서의 참조가 조용히 어긋난다.
참조는 제목으로 한다(`DECISIONS.md "짧은 꼬리 이름 매칭"`).

## 짧은 꼬리 이름 매칭 - 기각

**대안:** `CompleteButton.TOGGLE`처럼 풀네임의 꼬리만으로 핸들러를 등록하고, 모호할 때만
더 긴 경로를 요구.

**기각 이유:** 같은 짧은 이름이 트리의 다른 상태에 따라 유효해지거나 무효해진다. 같은 핸들러
이름이 어떤 파일에선 되고 어떤 파일에선 안 되는 상황이 생겨 **예측 가능성을 해친다.** 정적
컴파일로 이름을 만든다는 전제의 최대 장점(컴파일타임 확정)을 스스로 깎는다.

**채택안:** 풀네임 강제. 유일성이 공짜로 보장되고, 이름이 곧 위치 정보이며, 리팩토링 시
깨짐이 조용히 숨지 않고 컴파일 에러로 드러난다.

긴 이름의 불편은 언어 규칙이 아니라 **툴링**(자동완성, 이벤트 카탈로그, 미처리 이벤트 경고)으로
해결한다. 풀네임 강제와 충돌하지 않는다 - 자동완성은 필수 기능으로 전제한다.

## 컨텍스트를 경로 마디에 포함 - 기각

**대안:** `MyTodoCard.TodoItem.ActionArea.CompleteButton.TOGGLE`처럼 `@with` 컨텍스트도
경로에 넣기.

**기각 이유:** 경로(누가 쐈나)와 컨텍스트(어떤 맥락에서 쐈나)는 직교하는 두 축이다. 섞으면
이름이 불필요하게 길어지고 두 축의 독립성이 무너진다.

**채택안:** 컨텍스트는 경로에서 빼고 `context`에 컨텍스트명을 키로 주입한다
(`context.ActionArea.userId`). 컨텍스트는 각자 이름공간을 가진다. 같은 이름이 중첩되는 것은
비정상이며(맥락은 중복이 없는 게 맞다), 합성 경계 너머 중첩은 런타임이 안쪽 우선으로 덮고
경고한다(ISSUES.md).

## 별칭 중복을 충돌 에러로 처리 - 기각

**대안:** 같은 풀네임이 두 번 생성되면 컴파일 에러, 또는 자동 인덱스(`TodoItem#0`) 부여.

**기각 이유:** 자동 인덱스는 순서에 의존해 리팩토링에 취약하다. 에러 처리는 "같은 의도의
컴포넌트를 함께 다루고 싶다"는 정당한 사용을 막는다.

**채택안:** 같은 풀네임을 **의도적 공유**로 재정의했다. 분리하고 싶으면 별칭으로 명시한다.
지금 규칙은 DESIGN.md "같은 풀네임 = 의도적 공유".

## 페이로드를 `any`로 방치 - 기각

**대안:** 핸들러의 `data`/`provided`를 `any`로 둠.

**기각 이유:** 풀네임은 강타입인데 페이로드가 `any`면 절반만 타입세이프하다.

**채택안:** `events` 블록의 페이로드 스키마가 이미 있으므로, 컴파일러가 이를 풀네임에 묶어
각 이벤트의 `data` 타입을 자동 추론한다. 컴파일 언어로 통합되면 자연스레 해결되는 영역으로 본다.

## 자기완결 요소(self-close) 표기 - `( /)` 채택

자식 없는 요소를 여는 태그 안 끝의 `/`로 닫는다(`img(src="a.png" /)`, `br( /)`).
문법 규칙은 SYNTAX.md #3.1.1.

**채택 이유:**

- **`/`를 괄호 안에 둔다** - 괄호가 여는 태그이므로 `/`도 그 안에 있어야 한다
  (HTML `<img />`와 같은 구조).
- **자식 없으면 self-close 필수, 빈 블록 금지** - 같은 상태를 두 가지로 쓸 수 있으면 표기가
  흔들린다. "자식 없음 = self-close" 한 가지로 고정한다.
- **void 요소도 컴파일타임에 차단** - 컴파일러가 void 집합을 아니까 런타임 방어를 둘 이유가 없다.
- **처음엔 엄격하게** - `/` 앞 공백 강제, `()` 축약 없음. 완화는 나중에 필요가 생기면 한다.
  반대 방향(느슨하게 시작해 조이기)은 이미 쓰인 코드를 깨뜨린다.

## 핸들러 선언 문법 - fullname을 통째로 쓰는 열린 구조 채택

핸들러는 컴포넌트와 분리된 선언이다 - 사용처에 묶이고, `get`/`set`이 부수효과라 순수 컴포넌트의
위치 독립성을 깬다.

**지금 상태:** 짝 TypeScript 파일에 객체 리터럴로 쓴다(`card.qubc` <-> `card.qubc.handlers.ts`,
키가 fullname). 타입과 자동완성은 편집기 확장이 주입한다. quble 자체 문법은 아직 아이디어
단계다(DESIGN.md 미결 "핸들러 문법" - `handle` 표기가 종착점 후보이나 바뀔 수 있다).

**확정된 것 - 어떤 문법이 되든 지켜야 할 것:**

- **fullname을 통째로 적는다** - 한 핸들러에서 "트리 어디서 난 무슨 이벤트"가 한눈에 들어와야
  한다.
- **항목마다 독립** - 핸들러 하나를 추가하는 것이 다른 핸들러를 건드리지 않는다. 추가하는
  도중에도 문법이 깨지지 않는다.

**기각한 대안:**

- **경로 블록 중첩** - prefix를 묶어 트리 모양과 맞추지만, 한 핸들러에서 fullname이 조각나
  한눈에 들어오지 않는다.

      handlers {
        MyTodoCard.TodoItem {
          CompleteButton.TOGGLE (data) { ... }
        }
      }

- **객체 리터럴을 quble 문법으로 삼기** - 닫는 `}`까지 가야 문법이 완성돼 항목을 추가하는
  도중이 불안정하다. fullname이 문자열 키라서 문자열 안 자동완성과 트리 검증도 약하다. 지금
  TS 파일에서 이 형태를 쓰는 것은 quble 문법이 생기기 전까지의 임시다 - TS 쪽은 편집기 확장이
  타입을 주입해 검증을 메운다.

두 대안을 가른 기준은 자동완성이다 - 문법이 자동완성을 떠받쳐야 관련 도구(에디터 지원, 이벤트
카탈로그, 미처리 이벤트 경고)를 만들기 쉬워진다. "짧은 꼬리 이름 매칭"에서 정한 "긴 이름의
불편은 도구로 푼다"와 같은 줄기다.

## 이벤트 payload/context에 객체 전달 - 타입 테이블 + 런타임 조립 채택

핸들러가 payload/context로 값 한 칸이 아니라 객체를 통째로 받는 것. `events { SAVE({ user }) }`
에서 `user`가 `{ name: ..., email: ... }` 같은 객체면 핸들러의 `data.user`가 그 객체다.
전에는 값 자리에 한 칸짜리(문자열/숫자/불리언)만 담겨서 `{ user }`가 컴파일되지 않았다.

지금 계약은 core/BYTECODE.md, 조립하는 코드는 `core/web/runtime.ts`.

**채택 이유:**

- **바이트코드는 값을 담지 않고 모양과 출처만 담는다.** 값을 읽어 객체로 만드는 일은 런타임이
  한다. 칸에서 값을 읽어 모으는 일이라 store나 반응성을 건드리지 않는다 - 값 레이어에만 손댄다.
- **객체 모양은 모듈 전체에서 한 번만 적는다(타입 테이블).** 같은 모양이면 같은 번호를 함께
  쓴다. 그 모양을 어떤 순서로 조립할지는 런타임이 만들어 둔다. 파일에는 모양만 있으면 되고
  조립 순서는 실행할 때만 필요해서, 목적이 다른 둘을 따로 둔다.
- **이벤트가 발생할 때 객체를 다 만들어 넘긴다.** 필드 100개에 깊이 5짜리도 조립이 마이크로초
  수준이라 미룰 이유가 없다. 시간은 대부분 JS 객체를 만드는 데 쓰인다.

**기각한 대안:**

- **payload 전체를 시작 칸 하나로 가리키기** - payload를 store의 연속 구간 하나로 보고 시작 칸
  번호와 타입만 넘기는 안. 성립하지 않는다 - payload의 필드들은 출처가 제각각이라(어떤 필드는
  리터럴, 어떤 필드는 이 prop의 칸, 어떤 필드는 저 prop의 칸) store에 나란히 놓일 이유가 없다.
  그래서 필드마다 출처를 하나씩 싣는다. 필드 하나가 객체인 경우는 다르다 - 그 객체는 칸이
  붙어 있어서 시작 칸 하나로 가리키고 타입 크기만큼 편다.
- **조립 순서를 바이트코드에 넣기** - 런타임이 조립 순서를 만드는 일은 없어지지만, 같은 모양이
  payload마다 되풀이돼 파일이 커진다. 모양을 한 번만 적는 이점을 잃는다.
- **읽는 필드만 그때그때 만들기(Proxy)** - 객체의 일부만 읽을 때나 이득인데, 통째로 만드는
  비용이 이미 작다. 객체 전체를 쓸 때(직렬화 등)는 접근마다 가로채는 값이 붙어 오히려 몇 배 느리다.
- **재귀로 조립하기** - 코드는 단순하지만 객체가 깊으면 콜스택이 넘쳐 터진다. 막을 방법이 없는
  실패다. 반복문으로 스택을 직접 들고 돌면 깊이에 견딘다.
- **컴파일타임 leafIndex 고정** - 안 된다. `@for`가 요소를 늘리고 줄이면서 store 칸을 그때그때
  잡고 풀어서, 칸 번호를 컴파일할 때 미리 정할 수 없다. 조립은 scope index만 쓰고 실제 칸 번호는
  런타임이 발급한 것을 따른다.

## 배열 항목 식별자(key) - 도입 안 함

React `key` / Svelte `(id)`에 해당하는 것. `@for`에 항목마다 안정적 식별자를 적게 할지가
미결로 남아 있었다. 도입하지 않는다.

**필요 없는 이유 - 항목 정체를 이미 leafIndex가 짚는다.** `arrayInfo`가 요소별로 두 배열을
나란히 든다(`core/web/region.ts`):

- `elemStartLeafIndices[i]` - i번째 요소의 값이 사는 store 칸의 시작
- `indexLeafIndices[i]` - i번째 요소의 **회차 번호를 담은 칸**

인덱스가 요소를 가리키는 주소가 아니라, 요소가 가진 칸에 **담긴 값**이다. 몸체의 `{i}`/`$0`가
그 칸을 구독하므로, `removeAt`이 중간을 빼고 뒤 칸들을 `set`으로 당기면 인덱스 보간이 알아서
갱신된다. 항목 자신의 leafIndex는 그대로 살아 있다 - 위치가 바뀌어도 정체가 흔들리지 않으니
정체를 따로 붙일 이름이 필요 없다.

**재정렬(swap/move)도 key를 부르지 않는다.** 노드를 옮기지 않고 값만 맞바꾸기 때문이다
(#5 - 객체 swap은 그 객체가 품은 리프들의 값이 바뀐 것으로 환원). 회차 DOM과 구독은 위치에
그대로 있고 칸 값만 서로 `set`된다. 요소가 배열을 품으면 그 두 배열끼리 다시 같은 규칙을
적용한다 - 길이가 다르면 안쪽 회차가 그만큼 생기거나 없어질 뿐, 여기서도 노드를 옮기지 않는다.

**기각한 대안:**

- **노드 이동** - 회차 branch를 실제로 옮기고 구독까지 딸려 보내는 방식. 값 교체로 같은 결과가
  나오므로 지금은 쓰지 않는다. 닫힌 것은 아니다 - 값 교체는 바뀐 칸 수에 비례해 일하므로 요소가
  크면 옮기는 편이 싸질 수 있다. REACTIVITY.md #5.

## 표현식 테이블 - 컴포넌트 소유 + 후위 표기 채택

`@if (count > 0)`처럼 연산자가 붙은 조건을 담는 자리. 잎 하나짜리 조건은 기존 `IF`가
`(scope_index, offset)`으로 그대로 받고, 연산자가 붙은 식만 이 테이블을 거친다.

식은 **후위 표기**로 담고, 테이블은 **컴포넌트 테이블의 각 def가 소유**한다. 식 안에
`scope_index`를 그대로 박으므로 같은 모양이라도 컴포넌트가 다르면 다른 엔트리다 - 중복 제거는
한 컴포넌트 안에서만 일어난다.

**채택 이유:**

- **표현식은 재사용이 잘 안 된다.** 실제로 쓸 법한 페이지(주문 목록: 행/요약/페이지 컴포넌트)로
  재 보니 조건 표현식 11개 중 모양까지 같아 합쳐지는 건 2개뿐이었다. 중복 제거로 아낄 것이
  거의 없다.
- **중복 제거를 노리면 오히려 커진다.** 모듈 전역에서 합치려면 `scope_index`를 식에서 빼
  호출부가 인자로 실어 보내야 한다. 식에서 잎 하나당 1바이트를 아끼는 대신 호출부마다 2바이트가
  붙어서, 그 식이 두 번 넘게 쓰이지 않으면 손해다. 위 예시로 재면 컴포넌트 소유 115B, 전역에
  변수만 인자화 145B, 상수까지 인자화 153B였다.
- **소유가 분명해 인덱스가 짧아진다.** 식 번호가 컴포넌트 안에서만 유일하면 되므로 `u8`이면
  충분하고, `IF_EXPR`이 opcode 포함 2바이트로 끝난다.
- **후위 표기는 순회가 한 번씩이다.** 구독을 걸 때 한 번, 값을 셀 때 한 번, 각각 바이트를 앞에서
  뒤로 훑으면 된다. 재귀도 사이클 검사도 필요 없다.

**기각한 대안:**

- **표현식 테이블을 모듈 전역에 두기** - 상수풀/타입 테이블과 같은 줄에 세우는 안. 참조 방향은
  맞지만(코드가 테이블을 가리키고 그 반대는 없다) `scope_index`가 식에 박히는 한 컴포넌트를
  넘어 합쳐지지 않아, 전역에 두고도 중복 제거를 못 얻는 어정쩡한 자리가 된다.
- **잎을 인자로 빼기** - `IF_EXPR`이 `(scope_index, offset)` 목록을 싣고 식은 `LoadArg n`만 담는
  안. 중복 제거의 전제인데 위에 적은 이유로 총량이 늘었다.
- **이항 노드마다 엔트리를 만들고 피연산자를 다른 엔트리 참조로 두기** - 부분식을 공유하자는
  안. 잎까지 내려가면 결국 `scope_index`가 박혀 인자화 없이는 공유가 안 되고, 피연산자가 `u16`
  참조로 바뀌면서 짧은 식은 오히려 커진다. 참조가 생기면 순회도 다시 재귀가 되어 후위 표기로
  얻은 이점을 잃는다.
- **점프/단락 평가(short-circuit)** - 값 자리에는 부수효과가 없어 다 세어도 결과가 같다.
  BYTECODE.md 5.1의 "왜 점프가 없어야 하는가"와 같은 이유.
