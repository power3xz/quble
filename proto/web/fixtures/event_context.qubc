component C {
  props { userId: string }
  contexts { Area { section: "actions", userId: userId } }
  events { TOGGLE({ userId }) }
  template {
    @with Area {
      button(@click:TOGGLE) { "x" }
    }
  }
}
