component C {
  props { userId: string }
  contexts {
    Outer { area: "outer", userId: userId }
    Inner { area: "inner", tier: "gold" }
  }
  events { TOGGLE({ userId }) }
  template {
    @with Outer {
      @with Inner {
        button(@click:TOGGLE) { "x" }
      }
    }
  }
}
