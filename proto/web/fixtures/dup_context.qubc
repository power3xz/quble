use Child from "./dup_context_child.qubc"

component Parent {
  props { userId: string }
  contexts { Area { tier: "silver", scope: "page" } }
  template {
    @with Area {
      Child(userId={userId}) {}
    }
  }
}
