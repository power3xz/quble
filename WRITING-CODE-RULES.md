# 코드 작성 규칙

## 1. 시그니처 먼저

새 함수는 이름/인자/반환값을 먼저 제시하고 승인받은 뒤 구현한다. 여러 개면 목록으로 한 번에.

계획 승인은 시그니처 승인이 아니다.

## 2. 확인하고 쓴다

이름, 시그니처, 자료구조, 가시성(private/pub) - 추측해서 쓰지 않는다. 열어 보고 쓴다.

없는 것을 지어내면 컴파일이 안 되고, private은 모듈 밖에서 못 쓴다.

## 3. 테스트 작성 -> 구현 -> 통과 확인 -> 기존 테스트 회귀 검증

이 순서가 한 스텝이다. 넷이 끝나야 그 스텝이 끝난다.

테스트를 먼저 쓰는 것은 무엇이 맞는지를 구현 전에 못박기 위해서다. 구현을 보고 테스트를
쓰면 구현이 하는 일을 그대로 옮겨 적게 되고, 그러면 그 테스트는 아무것도 못 잡는다.

시그니처는 1절에서 이미 합의했으므로 빈 구현(`todo!()`)을 두고 테스트를 쓴다 - 컴파일이
되어야 테스트를 붙일 자리가 생긴다는 문제는 그것으로 해소된다.

**새로 쓴 테스트가 실패하는 것을 먼저 본다.** 안 그러면 그 테스트가 무엇을 잡는지 알 수 없다.
버그 수정도 같다 - 고치기 전에 그 버그로 실패하는 테스트를 만든다.

기존 테스트가 통과하는 것은 **원래 되던 것이 안 망가졌다**는 뜻이지, 이번에 만든 동작이
검증됐다는 뜻이 아니다. 새 경로에는 그 경로를 겨냥한 새 테스트를 쓴다.

요소를 각각 덮은 것으로 안심하지 않는다 - 조합과 경계를 덮는다.

테스트는 실제 코드 경로로 검증한다 - 흉내낸 복제본을 검사하면 그 복제본만 맞다.

**방금 쓴 테스트만 돌리고 끝내지 않는다.** 전체를 돌려 다른 곳이 안 깨졌는지 본다.

자동 테스트가 못 보는 것(화면, 상호작용)은 **사용자에게 직접 확인을 요청한다.** 무엇을
어떻게 보면 되는지 적어서 부탁한다 - 확인 없이 됐다고 하지 않는다.

## 4. 주석 작성할 때

**간결하게 핵심만.** 코드를 읽으면 아는 것은 적지 않는다. 적을 것은 코드에 안 드러나는 것 -
왜 이 호출이 여기 있는지, 왜 이 순서인지다.

**설명이 필요한 코드는 구현이 확정된 뒤에 적는다.** 값을 만지는 중에 붙인 주석은 다음 수정에서
곧 낡는다.

**설명 뒤에 짧은 예시를 붙인다.** 형태/순서/변환처럼 예시 한 줄로 보일 수 있으면 설명을 길게
잇는 대신 그것으로 마친다. 예시로 담기 어려운 것(이유, 제약)은 설명으로 둔다.

| O | X |
|---|---|
| `// 중간을 빼면 뒤 인덱스가 당겨진다. [a, b, c] -> removeAt(1) -> [a, c]` | `// 중간 요소를 빼면 그 뒤 요소들의 인덱스가 하나씩 앞으로 당겨지고, 목록의 길이도 그만큼 줄어든다` |
| `// 필드 순서대로 값만 편다. { a: 1, b: 2 } -> [1, 2]` | `// 객체의 필드를 선언 순서대로 훑어 값만 배열로 뽑아낸다` |
| `// 마디로 자른다. "a.b.c" -> ["a", "b", "c"]` | `// 점으로 구분된 경로 문자열을 마디 단위로 잘라 배열로 만든다` |
| `// 안쪽 회차 수도 서로 맞바뀐다. 3장 <-> 1장이면 1개로 줄고 3개로 는다` | `// 두 배열의 길이가 다를 때는 각 배열을 순회하는 반복의 회차 수가 서로의 길이에 맞춰 늘거나 줄어야 한다` |
