# 기술 요소 정리

## 실행 모델 결정 (확정)

**바이트코드 + VM이 본체.** 컴파일 산출물은 바이트코드이고, 언어 독립적 VM이 서버·클라이언트
양쪽에서 실행한다. React/Svelte/Vue 등 프레임워크 target(아래 11)은 **나중에 제공할 옵션**으로
격하한다 - 둘은 한 컴포넌트에서 양립 불가하며, 바이트코드를 본체로 택했다.

> 근거: "기술 중립(어떤 언어로도 구현 가능)" 요건이 트랜스파일-본체 설계를 사실상 배제한다.
> 바이트코드/VM 스펙이 중립성을 달성하는 경로다.

**다음 구체화 대상: A(컴파일러 프론트엔드)** 부터.

---

## 의존 순서

```
A(프론트엔드) ──┬─-> B(바이트코드+VM) ──-> C(반응성) ──-> D(SSR/프로토콜)
                └─-> (옵션, 후순위) 프레임워크 백엔드 - 11
        ↑
   먼저 확정 가능
```

---

## A. 컴파일러 프론트엔드 - 항상 필요, 먼저 확정 가능

- **1.** 컴파일로 정적 분석이 가능해야한다.(provided는 트리 분석으로 타입을 결정)
- **6.** 이벤트이름은 컴파일 타임에 결정된다.
- **7.** 이벤트 핸들러의 매개변수타입도 컴파일 타임에 결정된다.
- **11(전반부).** 컴파일시 내부적으로 ast를 생성한다. (AST는 공용 IR - target 선택만 백엔드 문제)

## B. 바이트코드 백엔드 - 본체

- **3.** 컴파일 결과는 바이트코드이다.
- **4.** 바이트코드는 opcode, operand 등으로 구성되어있다.(CREATE_ELEMENT 같은 명령어가 바이트 코드로 인코딩)
- **8.** 문자열 리터럴은 상수풀로 컴파일되고 참조시 풀 탐색.
- **9.** 알려진 html요소는 미리 정의된 상수풀 아이디로 사용. (8·9는 바이트코드 포맷 세부)
- **12.** 바이트 코드를 실행할 vm이 필요하다.(서버, 클라이언트에서 모두 동작 가능해야함)

## C. 런타임 - 반응성 / 로딩

- **5.** 컴포넌트의 데이터 전달을 추적하여 반응성에 활용한다. (반응성 모델 결정됨: REACTIVITY.md. 남은 건 §5.1 배열 요소 식별/key.)
- **2.** 동적으로 컴포넌트 로드가 가능해야한다.

## D. 분산 / 프로토콜 - 가장 후행

- **10.** 서버까지 아우르는 프로토콜 필요
  - 최초 페이지 요청시 html을 렌더링 (SSR)
  - 필요한 컴포넌트 요청시 컴파일된 바이트코드 응답. 클라이언트에서 렌더링
- **기술적인 중립 필요.(어떤 언어에서나 구현 가능해야함)** - D만의 요건이 아니라 B·C 전체에 거는 제약.
  바이트코드 포맷·VM 명세가 언어 독립적 스펙이어야 한다는 뜻.

---

## 후순위 옵션

- **11(후반부).** target으로 native, react, svelte, vue 등 선택 가능(각 프레임워크별 ast에서 생성하는
  방법 정의 필요). -> 바이트코드/VM/반응성이 자리잡은 뒤 export 기능으로.
